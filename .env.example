# Copy to .env and fill in — .env is gitignored, this file is just documentation.
# Loaded manually (export / set, or a tool like python-dotenv) — ingestion/fetch_waqi.py
# reads it via os.environ.get("WAQI_TOKEN"), no auto-loading is wired up.

# Free token, ~10 seconds, no approval wait: https://aqicn.org/data-platform/token/
WAQI_TOKEN=
